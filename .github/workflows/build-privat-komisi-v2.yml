name: Build PrivatKomisi APK
on:
  push:
    branches:
      - build-privat-komisi-v2
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Install Android SDK
        run: yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" "platforms;android-35" "build-tools;35.0.0"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Restore signing keystore
        working-directory: privat-komisi-android/app
        run: base64 -d privatkomisi.keystore.b64 > privatkomisi.keystore
      - name: Build signed release APK
        working-directory: privat-komisi-android
        run: gradle :app:assembleRelease --no-daemon --stacktrace
      - name: Verify APK signature and package
        run: |
          "$ANDROID_HOME/build-tools/35.0.0/apksigner" verify --verbose --print-certs privat-komisi-android/app/build/outputs/apk/release/app-release.apk
          "$ANDROID_HOME/build-tools/35.0.0/aapt" dump badging privat-komisi-android/app/build/outputs/apk/release/app-release.apk | head -8
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: PrivatKomisi-v8-whatsapp
          path: privat-komisi-android/app/build/outputs/apk/release/app-release.apk
          retention-days: 1
